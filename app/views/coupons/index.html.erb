
<h1>All Coupons Available!</h1>
 <div>
  <%@coupons.each do |coupon| %>
  <div>
    <ul>
    <li><%= link_to coupon.coupon_code, coupon_path(coupon) %></li>
    <li><%= link_to coupon.store, coupon_path(coupon) %> </li>
    <% end %>
    </ul>
  </div>
